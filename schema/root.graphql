type Query {
    user(id: Int): User
    tasks(
        first: Int,
        after: String,
        last: Int,
        before: String,
        query: String
    ):TaskConnection!
    items(
        first: Int,
        after: String,
        last: Int,
        before: String,
        query: String
    ):ItemConnection!
}
